import { AreaestudioPipe } from './areaestudio.pipe';

describe('AreaestudioPipe', () => {
  it('create an instance', () => {
    const pipe = new AreaestudioPipe();
    expect(pipe).toBeTruthy();
  });
});
