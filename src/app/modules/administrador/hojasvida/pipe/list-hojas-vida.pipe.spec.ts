import { ListHojasVidaPipe } from './list-hojas-vida.pipe';

describe('ListHojasVidaPipe', () => {
  it('create an instance', () => {
    const pipe = new ListHojasVidaPipe();
    expect(pipe).toBeTruthy();
  });
});
