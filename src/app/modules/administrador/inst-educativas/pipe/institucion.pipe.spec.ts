import { InstitucionPipe } from './institucion.pipe';

describe('InstitucionPipe', () => {
  it('create an instance', () => {
    const pipe = new InstitucionPipe();
    expect(pipe).toBeTruthy();
  });
});
