import { OfertasLaboralesPipe } from './ofertas-laborales.pipe';

describe('OfertasLaboralesPipe', () => {
  it('create an instance', () => {
    const pipe = new OfertasLaboralesPipe();
    expect(pipe).toBeTruthy();
  });
});
