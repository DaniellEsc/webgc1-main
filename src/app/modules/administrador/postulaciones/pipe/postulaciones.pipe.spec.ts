import { PostulacionesPipe } from './postulaciones.pipe';

describe('PostulacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new PostulacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
