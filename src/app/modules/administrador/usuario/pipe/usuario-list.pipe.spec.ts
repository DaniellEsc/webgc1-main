import { UsuarioListPipe } from './usuario-list.pipe';

describe('UsuarioListPipe', () => {
  it('create an instance', () => {
    const pipe = new UsuarioListPipe();
    expect(pipe).toBeTruthy();
  });
});
